
/**
 * screenshot_init()
 * Initializes the screenshot module
 */
export const screenshot_init = ():void => {}

/**
 * screenshot_update()
 * Checks if the user wants to take a snapshot, and if
 * he/she does, we must do it.
 */
export const screenshot_update = ():void => {}

/**
 * screenshot_release()
 * Releases this module
 */
export const screenshot_release = ():void => {}
